<div class="container mt-4">

  <!-- ▼ 戻るボタン -->
  <div class="mb-3">
    <%= link_to "← コミュニティ詳細に戻る",
          community_path(@community),
          class: "btn btn-outline-secondary" %>
  </div>

  <h3 class="text-center mb-4">
    <%= @community.name %> の参加メンバー
  </h3>

  <% if @members.any? %>

    <div class="row justify-content-center">

      <% @members.each do |user| %>
        <div class="col-md-4 mb-3">

          <div class="card p-0 text-center"> <!-- p-0でカードの内側パディングを削除 -->

            <!-- バッジ（幅いっぱい） -->
            <div class="w-100 py-2 <%= user == @community.owner ? 'bg-warning' : 'bg-primary' %>">
              <span class="text-white fw-bold">
                <% if user == @community.owner %>
                  👑 オーナー
                <% else %>
                  メンバー
                <% end %>
              </span>
            </div>

            <!-- 画像と名前を横並び -->
            <div class="d-flex align-items-center p-3" style="min-height: 120px;">
              
              <!-- 画像（左端） -->
              <div class="me-5">
                <% if user.profile_image.attached? %>
                  <%= image_tag user.profile_image,
                        size: "80x80",
                        class: "rounded-circle" %>
                <% else %>
                  <%= image_tag "default-image.png",
                        size: "80x80",
                        class: "rounded-circle" %>
                <% end %>
              </div>
              
              <!-- 名前（右側） -->
              <div class="flex-grow-1 text-start">
                <h5 class="fw-bold mb-0">
                  <%= link_to user.name,
                        user_path(user),
                        class: "text-decoration-none fw-bold" %>
                </h5>
              </div>
              
            </div>

          </div>

        </div>
      <% end %>

    </div>

  <% else %>

    <div class="alert alert-info text-center">
      まだ参加メンバーはいません
    </div>

  <% end %>

</div>