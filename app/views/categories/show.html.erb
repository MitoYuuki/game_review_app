<div class="container mt-4">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8">

      <h2 class="mb-2 text-center"><%= @category.name %></h2>

      <hr>

      <% if @categories.present? %>
      <% end %>

      <% if @communities.any? %>
        <% @communities.each do |community| %>

          <div class="card mb-3">
            <div class="card-body">

              <!-- コミュニティ名 -->
              <h5 class="card-title text-truncate">
                <%= link_to community.name,
                            community_path(community),
                            class: "text-decoration-none" %>
              </h5>

              <!-- 説明 -->
              <p class="card-text">
                <%= truncate(community.description, length: 80) %>
              </p>

              <small class="text-light">
                作成者：
                <%= link_to community.owner.name,
                            user_path(community.owner),
                            class: "text-info" %>
              </small>

              <div class="small mt-1">
                メンバー: <%= community.members.count %>人 /
                トピック: <%= community.topics.count %>件
              </div>

            </div>
          </div>

        <% end %>
      <% else %>

        <p class="text-center text-light">
          このカテゴリに属するコミュニティはまだありません。
        </p>

      <% end %>

    </div>
  </div>
</div>
